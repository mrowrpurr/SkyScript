#pragma once

#include "specHelper.h"

go_bandit([](){
    describe("Variable Declaration and Assignment", [](){
        xit("can declare a primitive __string__ variable", [&](){
//            auto context = ContextImpl();
//            AssertThat(context.FunctionCount(), Equals(0));
//            AssertThat(context.FunctionExists("hello"), IsFalse());
//
//            Eval(context, R"(
//hello():
//)");
        });
    });
});
