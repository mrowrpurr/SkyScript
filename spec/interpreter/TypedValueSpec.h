#pragma once

#include "specHelper.h"

go_bandit([](){
    describe("TypedValue", []() {
        // e.g. any type's fields should store a TypedValue which may be a primitive of a complex type.
        xit("somehow stores typed values!", [&](){});
    });
});
